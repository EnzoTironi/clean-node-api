{
  "name": "clean-node-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --colors --passWithNoTests --silent",
    "teste:unit": "npm test -- jest --watch ",
    "teste:staged": "npm test -- --findRelatedTests",
    "test:ci": "npm test -- --coverage ",
    "prepare": "husky install"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "husky": "^7.0.0",
    "jest": "^27.4.7",
    "lint-staged": "^12.1.7",
    "standard": "^16.0.4",
    "ts-node": "^10.4.0"
  }
}
